<!DOCTYPE html>
<html lang="pl-PL">
<head>
    <meta charset="UTF-8">
    <title>Demo Test</title>
    <link rel="stylesheet" href="/css/style.css">
    <meta name="viewport" content="width=device-width,initial-scale=1" />

</head>
<body>
<header>

    <% if(!loggedUser) { %>
        <div id="login">
            <h5>Logowanie</h5>
            <form action="/login" method="post" novalidate>
                <label for="loginEmail">Email: </label>
                <input type="email" id="loginEmail" name="email">

                <label for="loginPassword">Hasło: </label>
                <input type="password" id="loginPassword" name="password">

                <input type="submit" value="Zaloguj sie">
            </form>
        </div>
    <% } else { %>
        <span>Jestes zalogowany: <%= loggedUser.firstName + " " + loggedUser.lastName%></span>
        <a href="/logout"><%= __('nav.logOut') %></a>
    <% } %>


    <h1>
         <%= __('main-page.content') %>
    </h1>
    <img src="/images/logo.png" alt="Demo Test">
</header>
<nav>
    <ul>
        <li><a href="/" class="<%= navLocation == 'main' ? 'active' : '' %>"><%= __('nav.main-page') %></a></li>
        <li><a href="/employees" class="<%= navLocation == 'emp' ? 'active' : '' %>"><%= __('nav.employees') %></a>
        <li><a href="/departments" class="<%= navLocation == 'dept' ? 'active' : '' %>"><%= __('nav.departments') %></a>
        <li><a href="/employments" class="<%= navLocation == 'employment' ? 'active' : '' %>"><%= __('nav.employments') %></a>
        <li><a href="/changeLang/pl" class="<%= lang == 'pl' ? 'active' : ''%>">PL</a></li>
        <li><a href="/changeLang/en" class="<%= lang == 'en' ? 'active' : ''%>">EN</a></li>
    </ul>
</nav>